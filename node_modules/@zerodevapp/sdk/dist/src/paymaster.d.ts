import { UserOperationStruct } from "@zerodevapp/contracts";
import { PaymasterAPI } from "./PaymasterAPI";
export declare class VerifyingPaymasterAPI extends PaymasterAPI {
    readonly projectId: string;
    readonly paymasterUrl: string;
    readonly chainId: number;
    constructor(projectId: string, paymasterUrl: string, chainId: number);
    getPaymasterAndData(userOp: Partial<UserOperationStruct>): Promise<string | undefined>;
}
